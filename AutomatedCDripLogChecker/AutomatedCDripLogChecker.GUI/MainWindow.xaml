<Window x:Class="AutomatedCDripLogChecker.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutomatedCDripLogChecker.GUI"
        mc:Ignorable="d"
        Title="Automated CDrip Log Checker" MinHeight="400" MinWidth="600"
        Height="400" Width="600" AllowDrop="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5"/>
            <RowDefinition/>
            <RowDefinition Height="5"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="5"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="3" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Button Content="Open" Command="{Binding OpenLog}"/>
            <Button Content="Open Folder" Command="{Binding OpenFolder}" Grid.Row="2"/>
            <Button Content="More Info" Command="{Binding MoreInfo}" Grid.Row="4"/>
            <Button Content="Clear" Command="{Binding ClearCollection}" Grid.Row="6"/>
            <Button Content="Export" Command="{Binding Export}" Grid.Row="8"/>
        </Grid>
        <ListView Grid.Column="1" Grid.Row="1" ItemsSource="{Binding LogModels}" SelectedIndex="{Binding SelectedId}">
            <ListView.View>
                <GridView AllowsColumnReorder="true"
              ColumnHeaderToolTip="Log Information">
                    <GridViewColumn DisplayMemberBinding=
                          "{Binding LogName}" 
                      Header="Log Name" Width="300"/>
                    <GridViewColumn DisplayMemberBinding=
                          "{Binding LogScore}" 
                      Header="Score" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
